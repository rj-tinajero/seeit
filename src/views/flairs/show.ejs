<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      
      <h1><%= flair.name %></h1>
      <p><%= flair.color %></p>
      <a href="/topics/<%= topicId %>/posts/<%= flair.postId %>/flair/<%= flair.id %>/edit" class="btn btn-warning">Edit</a>
      <form style="display:initial" action="/topics/<%= topicId %>/posts/<%= flair.postId %>/flair/<%= flair.id %>/destroy" method="post">
        <button type="submit" class="btn btn-danger">Delete Flair</button>
      </form>
      
      
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>